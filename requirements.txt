# Termux Intelligent Assistant — Python dependencies
# Install with:  python -m pip install -r requirements.txt
#
# Sovereign Core edition: zero cloud SDK dependencies.
# All backends (sovereign, ollama) use stdlib urllib — no pip packages required.
#
# Optional extras (uncomment to enable):

# Rich terminal output — coloured, formatted responses
# rich>=13.7

# Faster JSON parsing — useful on low-RAM Android devices
# ujson>=5.9

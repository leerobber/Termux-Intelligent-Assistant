# Termux Intelligent Assistant — Python dependencies
# Install in Termux with:  python -m pip install -r requirements.txt

# Cloud provider SDKs (required for the respective backends)
anthropic>=0.25.0
mistralai>=1.0.0
groq>=1.0.0

# Faster JSON (optional — speeds up large responses)
# ujson>=5.9

# Rich terminal output (optional — enables coloured, formatted responses)
# rich>=13.7
